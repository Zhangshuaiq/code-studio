import { useEffect, useState } from 'react'; import { ArrowLeft, Save } from 'lucide-react'; import { EditorContent, useEditor } from '@tiptap/react'; import StarterKit from '@tiptap/starter-kit'; import Placeholder from '@tiptap/extension-placeholder'; import { Markdown } from '@tiptap/markdown'; import { TableKit } from '@tiptap/extension-table';
import { useKnowledgeDocument, useKnowledgeMutations, useKnowledgeTree } from '../../hooks/useKnowledge'; import { useParams } from 'react-router-dom'; import { Select } from '../common/Select'; import { useFeedback } from '../common/FeedbackProvider'; import { errText } from '../Settings/ui';
export function KnowledgeDocumentPage(){const{id}=useParams();const query=useKnowledgeDocument(id);const doc=query.data;const tree=useKnowledgeTree(doc?.teamId);const mutations=useKnowledgeMutations(doc?.teamId);const{toast}=useFeedback();const[content,setContent]=useState('');const[summary,setSummary]=useState('');const editor=useEditor({extensions:[StarterKit,TableKit.configure({table:{resizable:true}}),Placeholder.configure({placeholder:'开始编写团队知识文档…'}),Markdown],content:'',contentType:'markdown',editorProps:{attributes:{class:'yuque-editor mx-auto min-h-full max-w-5xl px-10 py-8 outline-none'}},onUpdate:({editor:e})=>setContent(e.getMarkdown())});useEffect(()=>{if(editor&&doc&&editor.getMarkdown()!==doc.contentMarkdown){editor.commands.setContent(doc.contentMarkdown||'',{contentType:'markdown'});setContent(doc.contentMarkdown||'')}},[editor,doc?.id,doc?.contentMarkdown]);if(query.isLoading)return <div className="grid h-screen place-items-center text-sm text-muted">正在打开知识文档…</div>;if(!doc)return <div className="grid h-screen place-items-center text-sm text-muted">文档不存在或无权访问</div>;const save=async()=>{try{await mutations.saveDocument.mutateAsync({id:doc.id,baseVersion:doc.version,contentMarkdown:content,changeSummary:summary||undefined});setSummary('');toast('知识文档已保存',{tone:'success'})}catch(e){toast(errText(e),{title:'保存失败',tone:'error'})}};return <main className="flex h-screen flex-col overflow-hidden bg-white dark:bg-slate-950"><header className="border-b p-4 dark:border-slate-800"><div className="flex items-center justify-between"><div className="flex items-center gap-3"><button className="icon-btn" onClick={()=>window.close()} title="关闭文档"><ArrowLeft size={16}/></button><div><div className="text-[10px] text-indigo-600">{doc.team?.name} · {doc.requirement?'需求文档':'知识文档'}</div><h1 className="font-bold">{doc.title}</h1></div></div><button className="btn btn-primary" disabled={content===doc.contentMarkdown||mutations.saveDocument.isPending} onClick={()=>void save()}><Save size={14}/>保存</button></div><div className="mt-3 flex items-center gap-3"><span className="text-xs text-muted">所属文件夹</span><Select size="sm" className="w-56" value={doc.folderId||'root'} options={[{value:'root',label:'知识库根目录'},...(tree.data?.folders||[]).map(x=>({value:x.id,label:x.name}))]} onChange={value=>void mutations.updateDocument.mutateAsync({id:doc.id,folderId:value==='root'?'':value})}/><span className="text-[10px] text-muted">创建人 {name(doc.createdBy)} · 最后更新 {name(doc.updatedBy)} · v{doc.version}</span></div></header><section className="min-h-0 flex-1 overflow-y-auto"><EditorContent editor={editor}/></section><div className="border-t p-3 dark:border-slate-800"><input className="input w-full" value={summary} onChange={e=>setSummary(e.target.value)} placeholder="本次修改说明（可选）"/></div></main>}
const name=(u?:{username:string;displayName?:string|null})=>u?.displayName||u?.username||'—';
