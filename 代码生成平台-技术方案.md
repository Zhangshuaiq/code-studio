# AI 代码生成平台——当前实现技术方案

> 文档基线：2026-09-01。本文依据当前仓库源码静态整理，描述已经落地的实现，而非路线图。启动、配置和运维命令见 [README](./README.md)，可观测性细节见 [observability-plan](./docs/observability-plan.md)。

## 一、系统定位与实现边界

平台面向多用户提供“创建项目 → AI 生成/修改 → 在线编辑 → 沙箱预览 → Git 协作 → 环境部署”的交付闭环，并包含团队、RBAC、审计、数据源工作台、数据库变更审批、Java 定时任务、业务日志和基础可观测能力。

代码编辑区采用需求级预览联调：需求是 Markdown 文档、排期、五阶段流水线、负责人、关联项目、开发分支和联调路由的主实体，全局唯一需求编号直接作为路由标识；用户工作区继续独立，需求协作者共享需求级联调覆盖。当前代码已完成需求聚合模型、权限、文档修订、阶段流转、关联项目、开发者需求分支、当前工作区不可变构建快照及 Kubernetes 预览标识，但尚未部署，也尚未接入实际流量路由适配器。完整边界见 [`docs/requirements-pipeline.md`](./docs/requirements-pipeline.md) 和 [`docs/preview-environment-routing.md`](./docs/preview-environment-routing.md)。

项目创建支持空白工作区和 Git 仓库导入两种模式。Git 导入仅接受不含 URL 凭据的 HTTPS 地址：公开仓库匿名拉取，私有仓库按当前用户和仓库主机读取加密保存的个人 Token。调用方未指定分支时，服务端通过远程 `HEAD` 识别默认分支；识别结果会在导入成功事务中同时写入 `ProjectRemote.branch` 和创建者的 `Session.workspaceBranch`，无法识别时返回 `GIT_DEFAULT_BRANCH_UNRESOLVED` 并要求显式填写。项目先以 `importing` 状态写入，完成工作区初始化和目标分支拉取后才转为 `active`；失败立即清理数据库记录与共享卷目录，超过 30 分钟的中断导入由资源回收 Worker 接管。

源码保存在共享工作区，模型 Provider 由独立 Worker 调用，依赖安装、构建验证和预览统一进入 Kubernetes 隔离环境。平台持有 Git 凭据、数据源凭据和 kubeconfig 等敏感能力，均应按生产密钥与最小权限资源管理。

| ID | 技术 | 生成/验证镜像 | 预览方式 | 部署方式 |
| --- | --- | --- | --- | --- |
| `react-vite` | React 18 + Vite | `sandbox-node:1.0` | Vite + HMR | 静态产物镜像 |
| `java` | Spring Boot 3 / Java 17 | `sandbox-java:1.0` | HTTP 服务 | 容器或 JAR/WAR 直传 |
| `node` | Express | `sandbox-node:1.0` | HTTP 服务 | 容器镜像 |
| `python` | FastAPI | `sandbox-python:1.0` | HTTP 服务 | 容器镜像 |
| `react-native` | Expo / Android | `sandbox-react-native:1.0` | 构建容器 + 模拟器 | Android 容器编排 |

四类运行时沙箱 Dockerfile 均已在 `sandbox-images/` 提供。React 静态运行镜像仍由部署模板引用 `sandbox-python:1.0`。

## 二、总体架构

```text
React Web
  ├─ 工作区：Chat / Monaco / 文件树 / Preview / Git
  ├─ 任务、部署、数据库、定时任务
  └─ 日志、指标、Trace、健康和管理后台
                 │ HTTP / SSE
                 ▼
NestJS API
  ├─ JWT + RBAC + 项目角色 + 审计 + Redis 限流
  ├─ 项目、会话、工作区、文件、Git
  ├─ 预览与部署 ───── Docker / Kubernetes / SSH
  ├─ 数据源与审批 ─── MySQL / PostgreSQL / Redis / MongoDB
  └─ 观测查询 ─────── OpenSearch / Tempo / Prometheus
          ├─ PostgreSQL：元数据和持久任务状态
          └─ Redis / BullMQ：生成队列和 Java 任务队列
                 │
                 ▼
NestJS Worker
  ├─ AI Provider：OpenAI-compatible simple / Aider
  ├─ 生成验证：Kubernetes Job
  ├─ Java 任务到期扫描、执行、重试、取消和心跳
  ├─ 数据库迁移/备份/恢复任务调度
  └─ 项目文件与数据库资源回收
```

本地 `npm run dev` 同时启动 API、Worker、Web；Compose 的 `platform` profile 还编排 PostgreSQL、Redis、OpenSearch、Tempo、OTel Collector、Prometheus 和 cAdvisor；Helm 将三类应用拆分部署，并提供 HPA、PDB、Ingress、NetworkPolicy、迁移 Job、持久卷和 ExternalSecret 接口。

## 三、代码组织

### 3.1 后端

| 模块 | 当前职责 |
| --- | --- |
| `auth/admin/audit` | 注册登录、JWT 密钥环、用户角色权限、审计 |
| `project-access/project/team` | 平台权限与 owner/maintainer/developer/viewer 项目权限 |
| `session/workspace/files` | 每用户会话、Git worktree、受限文件读写与 ZIP |
| `agent` | 持久生成任务、BullMQ、SSE、配额、Provider 和构建门禁 |
| `sandbox/preview` | Docker 沙箱、K8s 预览、构建配额和生命周期 |
| `git/deploy` | Git 协作；Docker/K8s/SSH 部署及发布记录 |
| `datasource/db-query` | 数据源授权、查询、审批、备份、迁移和恢复 |
| `scheduled-task` | Java 方法注册、审批、Cron/单次/手动调度 |
| `business-log` | Token 接入、OpenSearch 检索、聚合、告警与保留 |
| `tracing/resource-metrics` | Tempo Trace 与 Prometheus 资源指标 |
| `platform-health/api-metrics` | 依赖健康、配额视图、HTTP 指标 |
| `crypto/rate-limit` | AES-256-GCM 换钥、Redis 分布式限流 |

API 与 Worker 使用不同根模块。API 装载控制器和生产队列，Worker 装载生成消费者、Java 到期任务扫描与数据库传输调度；二者共享 Prisma、加密、工作区、Git 和队列配置。预览、日志保留和告警等尚留在 API 的周期维护通过 PostgreSQL advisory lock 保证多副本单例执行。

### 3.2 前端

前端为 React 单页应用。React Router 管理工作区、任务、部署、运行资源、数据库审批、定时任务、日志、监控和管理后台；React Query 管理服务端状态，Zustand 管理本地 UI 状态；Monaco 用于源码与冲突处理，CodeMirror 用于 SQL，ECharts 用于监控图表。大型页面采用懒加载。

## 四、核心流程

### 4.1 身份与授权

登录公开；注册是否开放由 `SELF_REGISTRATION_ENABLED` 控制，首次管理员必须提交至少 32 位的 `INITIAL_ADMIN_TOKEN`，并由数据库 advisory lock 保证并发下只有一个首次管理员。JWT 默认 7 天，支持 `kid` 与历史密钥平滑轮换；每个请求读取数据库中的当前用户、状态、角色和权限，因此停用账户或调整角色即时生效。停用、删除或移除管理员角色时，在 Serializable 事务内检查至少保留一个启用中的管理员，避免并发管理操作导致平台锁死。用户角色 ID 必须真实存在且写入前去重；自定义角色包含未知权限键时直接拒绝，不再静默丢弃配置。

授权有两层：平台 RBAC 控制功能入口；项目角色控制具体项目的 read/edit/manage。共享项目为每位用户创建唯一 Session 和独立 worktree 分支。敏感凭据使用带 key ID 的 AES-256-GCM 密文保存，轮换接口在数据库事务内重加密。

多副本 API/Worker 通过 Redis 分布式锁串行化共享卷写操作。锁使用随机所有权令牌、原子释放、周期续租和 AsyncLocalStorage 进程内可重入，Redis Cluster key 使用 hash tag 固定槽位。文件保存、Git 写请求和生成任务使用 `workspace:<projectId>:<userId>`，同项目不同用户可并行；Git worktree 创建/删除、部署 worktree 和项目文件清理使用 `repository:<projectId>`；部署执行使用 `deployment:<projectId>:<bindingId>`。`WORKSPACE_LOCK_LEASE_MS` 控制租约，`WORKSPACE_LOCK_WAIT_MS` 控制竞争等待时间；Redis 仍是工作区写入的强依赖，锁丢失时操作不得被当作成功。

平台内置 `admin`、`developer`、`release-manager`、`ops-manager`、`viewer` 五个角色，并允许管理员创建自定义角色；用户拥有多个角色时取权限并集。前端权限入口集中定义在 `apps/web/src/lib/access.ts`：侧边栏会递归过滤无权限的子菜单并隐藏空分组，React Router 对直接输入 URL 的访问再次使用权限门禁，页面内的危险操作再按细粒度权限隐藏。前端控制仅改善交互，最终授权始终以后端 `PermissionsGuard` 和资源归属检查为准。

默认菜单分配由权限而非角色名决定：项目与生成任务需要 `project:read`，部署中心需要 `deploy:execute`，运行目标和 Namespace 只读视图需要 `deploy-target:manage`，镜像仓库、模型、监控、定时任务、Kafka 与各治理页面分别使用自身权限。Namespace 创建/删除、Pod 删除、Deployment 扩缩容/重启同时要求 `deploy-target:manage` 与 `namespace:manage`；仅有运行目标权限时仍可查看资源和 Pod 日志，但不会显示变更按钮。账户设置对所有已登录用户开放。新增页面时必须同时补齐权限目录、菜单声明、路由门禁、操作按钮和后端接口校验，避免只隐藏菜单但仍可通过 URL 或 API 越权访问。

### 4.2 生成任务

1. API 校验平台权限、会话编辑权、项目日额度和会话活跃任务。
2. PostgreSQL 创建 `Task`，BullMQ 以 Task ID 作为 Job ID 入队。
3. Worker 通过 PostgreSQL advisory lock 按团队和用户做并发准入。
4. `simple` Provider 解析模型流式文件标记；`aider` 启动子进程增量编辑。
5. 可选构建验证：创建 non-root、最小权限 ServiceAccount、只挂载当前 workspace subPath 的 Kubernetes Job。
6. 成功后记录变更、创建 Git 提交，并可自动预览；进度由 BullMQ QueueEvents 转为 SSE。

任务状态持久化，支持取消、超时、重试、优先级和死信重放。Worker 启动时修复中断状态。

### 4.3 工作区、文件和 Git

项目创建者使用主目录，协作者使用 `.data/workspaces` 下的独立 Git worktree。文件 API 通过规范化路径限制访问根目录以内，过滤隐藏文件和依赖目录，并限制在线读取大小。

Git 远程地址属于项目；提交身份和 HTTPS Token 属于用户且按主机隔离。平台支持初始化/导入、分支、ahead/behind、fetch、merge、三方冲突、回滚和 `force-with-lease` 推送。正式部署使用单独 detached worktree。

### 4.4 沙箱、预览与部署

Docker 沙箱按 Session 隔离，工作区绑定到 `/workspace`，配置 CPU/内存和空闲回收。Kubernetes 预览通过 BuildKit Job 构建推送临时镜像，再创建 Deployment、Service、Ingress 和 NetworkPolicy；构建记录持久化并按团队/用户控制并发。

运行目标支持 `local-docker`、`docker-tcp`、`docker-ssh`、`k8s` 和 `server-artifact`。目标配置按类型白名单保存，编辑时未重新提交的私钥、口令、kubeconfig 和监控凭据由服务端保留，显式 `null` 才执行清除。正式发布从独立 worktree 构建。产物服务器当前只处理 Maven JAR/WAR，通过 SFTP 临时上传后原子替换，并可执行显式重启命令。

### 4.5 数据源与数据库审批

数据源支持 MySQL、PostgreSQL、Redis、MongoDB，访问由 `DatasourceMember` 控制。详情接口只返回非敏感连接信息与 `hasPassword`，已保存密码不会回传浏览器，编辑留空会保留旧密码。数据源主机和数据库标识符经过 DTO 与服务层双重约束，MongoDB URI 对凭据、数据库和认证库分别编码；元数据查询及迁移预检不直接拼接未校验标识符，数据库驱动原始错误只写服务端日志。关系型只读 SQL 直接执行，写 SQL、CSV/XLSX 导入、跨库迁移和原生恢复进入审批；审批人必须是同项目组内已启用且已获数据源访问权的用户，关系型数据源始终至少保留一名审批人，申请人不能自批。

项目组成员移除在 Serializable 事务内同步撤销该组项目和数据源的显式授权及审批资格；若会移除关系型数据源最后一名审批人则整笔拒绝。删除项目组只允许空资源组：仍有关联项目、数据源或团队级部署目标时返回阻塞清单，避免数据库级联关系与界面提示不一致而误删数据源。

部署目标的 personal/team/platform 可见范围与团队归属由服务端校验；更新团队级目标的其它字段时保留原 teamId。Kubernetes 目标同时支持 deploy 与 preview，用于统一的 K8s-only 运行路线。kubeconfig 保存及使用时均执行安全校验，只允许内嵌凭据连接启用 TLS 校验的 HTTPS API Server，拒绝 exec/auth-provider、本地证书或 CA 文件引用、用户冒充、配置内代理及跳过 TLS；Registry 引用在保存时验证属于目标创建者且启用 TLS，共享目标部署时也按目标创建者解析仓库凭据且不静默降级。删除 Registry 时在 Serializable 事务中检查所有归属该用户的 K8s 目标加密配置及项目运行环境 JSON 配置，存在引用或无法安全解析目标配置时拒绝删除。删除目标则在 Serializable 事务中确认不存在沙箱和项目环境绑定，并对并发关联变化返回可重试冲突。

逻辑迁移用数据库租约、心跳和检查点协调，支持 fail/append/replace；迁移预检和 Worker 执行均再次校验数据库及表标识符。逻辑备份使用带版本的 manifest + 权限为 `0600` 的 JSONL。CSV/XLSX 导出同时限制行数和估算字节数，CSV 对公式起始字符进行文本转义；导入限制文件签名、ZIP 解压体积、条目数、工作表数、行列数、重复表头和单元格大小。原生备份调用 `pg_dump` 或 `mariadb-dump`，生成过程限制文件大小并在超限后清理临时文件；恢复前解析真实路径并验证 manifest 结构、大小、文件名、表清单、创建时间、SHA-256、引擎类型和目标空库。SQL、迁移和恢复审批均禁止申请人自批，失败记录不保存数据库驱动原始错误。

### 4.6 Java 定时任务

Java Starter 使用注册 Token 上报可调度方法；Handler 元数据采用嵌套 DTO 校验，布尔策略不做宽松真值转换。任务支持 immediate、once、cron，创建后需由其他审批人批准；调度器先关闭已超过 `expiresAt` 的任务，再扫描到期任务写入 BullMQ。Worker 调用应用内部端点，携带 Bearer Token、幂等键和 Trace ID，支持重试、超时、并发策略和取消。任务及执行历史使用统一分页协议。

### 4.7 可观测性

业务日志通过一次性 Source Token 接入 OpenSearch，Token 仅存 SHA-256。OpenSearch 查询具备统一超时和流式响应字节上限。OpenTelemetry 自动埋点输出 Trace/Metric 到 Collector；Trace 查询读取 Tempo，搜索参数使用 DTO 白名单，搜索与详情响应在转换前限制体积、Span 数、事件数、属性数和嵌套深度。资源指标读取 Prometheus，请求参数使用 DTO 白名单，响应限制体积、时间序列数、标签数和采样点数；Kubernetes 部署目标可声明经过校验的业务 Namespace 白名单。平台健康聚合 PostgreSQL、Redis/Worker、生成执行器、OpenSearch、Tempo 和 Prometheus。

## 五、持久化模型

Prisma schema 当前包含 30 个实体：

- 身份治理：`User`、`Role`、`AuditLog`、`SystemSetting`、`Team`、`ProjectMember`。
- 开发闭环：`Project`、`ProjectRemote`、`Session`、`Task`、`SandboxInstance`、`PreviewInstance`、`PreviewBuildRecord`。
- 凭据与发布：`ModelConfig`、`UserGitProfile`、`GitCredential`、`DeployTarget`、`Registry`、`ProjectRuntimeBinding`、`Deployment`、`DeploymentRecord`。
- 数据能力：`Datasource`、`DatasourceMember`、`DatabaseApproval`、`DatabaseTransfer`。
- 日志监控：`BusinessLogSource`、`MonitoringAlertRule`、`MonitoringAlertEvent`。
- Java 调度：`JavaTaskApplication`、`JavaTaskHandler`、`ScheduledJavaTask`、`JavaTaskExecution`。

PostgreSQL 是跨进程状态真源；Redis/BullMQ 保存队列执行态；OpenSearch、Tempo、Prometheus 分别承担日志、Trace、指标；源码和备份依赖共享持久文件系统。

## 六、关键配置和运行约束

- API 与 Worker 必须共用 PostgreSQL、Redis、密钥环和共享工作区。
- Redis 支持 standalone 与 Cluster；Cluster 使用 DB 0。
- `GENERATION_EXECUTOR=kubernetes`；必须提供经跨节点文件锁、原子 rename 和 Git worktree 语义验证的 RWX workspace PVC。
- 生产环境需明确 `WEB_ORIGIN`、`TRUST_PROXY`、外部服务认证和持久卷。
- Helm 默认关闭 Docker Socket；生产环境不得重新启用旧 Docker 执行路径。
- Helm 默认外部 egress 全放行，生产应收敛到模型、Git、数据库、队列和观测后端。

## 七、当前实现缺陷与改进优先级

### 已完成的 P0 加固

1. 管理员初始化已改为一次性 Token，移除了启动时自动提权；生产默认关闭后续自助注册。
2. SQL 工作台已增加危险语句识别、数据库只读事务、查询超时、结果行数和字节限制；Redis/MongoDB 工作台收敛为只读命令/操作。生产仍应使用数据库侧只读账号作为最终防线。
3. 数据源详情不再向浏览器回传明文密码；配置输入增加嵌套 DTO、端口和字段长度校验，编辑时密码留空采用服务端保留语义。

### P1：高优先级

1. **写接口已完成第二轮 DTO 化。** 数据库工作台、定时任务、部署、Git、项目/数据源成员、管理后台、团队、预览代理和 K8s 操作已接入运行时校验；后续需补 OpenAPI 契约测试，避免新增接口回退到 TypeScript-only 类型。
2. **后台调度已从 API 进程移出。** Java 到期扫描、数据库传输、预览回收与构建队列维护、日志保留和告警评估均只在独立 Worker 进程运行；API 入口强制使用 `PROCESS_ROLE=api`，Worker 入口强制使用 `PROCESS_ROLE=worker`。PostgreSQL advisory lock 继续作为多 Worker 副本的防重保障，不再占用 API 连接。
3. **自助注册权限已收敛。** 显式开放注册时默认角色为 `viewer`，可通过 `SELF_REGISTRATION_ROLE` 选择 `viewer` 或 `developer`；企业场景仍建议邀请制或管理员建号。
4. **项目删除已迁移到资源回收 Worker。** API 校验活动资源后只写入 `deleting`；Worker 通过 `deleting_cleanup` 原子认领，失败采用指数退避，达到 `PROJECT_CLEANUP_MAX_ATTEMPTS` 后进入 `deletion_failed`；管理后台支持记录确认人、时间、处置备注并人工重试，平台健康页展示失败告警；中断十分钟后允许其他实例接管。平台不提供静默忽略，避免资源残留失去追踪。
5. **依赖源、基础镜像入口和静态供应链门禁已配置化。** 运行时和部署 Dockerfile 不再硬编码第三方 npm/PyPI/Debian 镜像，可接入企业代理；沙箱 Dockerfile 提供基础镜像 Build Arg，版本和外部归档哈希进入机器可读清单；生产校验脚本拒绝缺失 digest 或仍使用可变 tag 的基础镜像。仍缺联网的依赖许可、漏洞和来源扫描。
6. **数据库持续同步尚未实现。** 当前 `DatabaseTransfer` 只支持一次性全量迁移、备份和恢复，不消费 MySQL binlog，也没有 GTID/position、事件幂等、schema 版本、延迟监控或死信重放状态。平台后续不直接开启数据库实例的 binlog，而是只读校验 DBA/云控制面完成的前置配置，并优先通过 Debezium 与持久事件通道接入独立 CDC Worker。完整边界和分阶段方案见 [`docs/database-cdc.md`](./docs/database-cdc.md)。本阶段仅记录设计，不实现。

### P2：工程化与可维护性

Kafka 可视化工作台已提供单集群 Topic/分区与配置管理、Consumer Group/Offset 管理、记录截断、受限消息抽样和审计发送；读取、发送、Topic 管理和 Group 管理权限分离，破坏性操作要求资源名二次确认。Topic 详情通过按时间戳查询起始 Offset，即时展示最近 5 分钟、1 小时和 24 小时的 Offset 增量；Group 详情用各分区最新 Offset 减去已提交 Offset，展示当前总延迟与分区延迟。Consumer Group 卡片同时展示在线成员数，并解码 Kafka 成员分配信息，列出 Client ID、来源地址和 Topic/分区；“工作中、空闲、重平衡中”由 Group 状态及当前分区分配推断，不代表消费者线程内部执行状态。所有位点使用十进制字符串传输并以 `BigInt` 计算，避免大位点精度丢失；时间窗口数据属于 Offset 估算值，不替代长期指标采集。

Topic 配置读取返回 Broker 的有效值、默认值来源、只读与敏感标记，敏感项不返回原值；页面为白名单参数提供中文用途、单位、特殊值与风险说明，其他 Kafka 原生参数明确标记为仅展示。写入采用固定 DTO 字段到 Kafka 配置名的映射白名单，不接受调用方传入任意配置键。当前白名单覆盖 retention、cleanup policy、segment、最大消息、最小同步副本、compression、compaction lag 和 timestamp type。

配置写入前会同时读取 Topic 元数据与当前有效配置，校验最小同步副本不超过实际副本数，并以“当前值与本次修改合并”的结果校验最小/最大 Compaction 延迟关系。Kafka 管理连接包装器保留领域 `HttpException`，只有真实客户端或 Broker 异常才统一映射为 `KAFKA_UNAVAILABLE`。

Topic 创建使用结构化表单代替浏览器顺序 Prompt，可在创建时设置分区、副本、最小同步副本及常用保留/压缩参数。DTO 与页面同时执行范围校验，服务端再次校验最小同步副本不超过新 Topic 副本数；KafkaJS 返回未创建时映射为 `KAFKA_TOPIC_ALREADY_EXISTS`，避免客户端收到假成功。

1. **没有 OpenAPI/Swagger。** 接口契约依赖源码、DTO 和手写文档，仍存在前后端漂移风险。
2. **业务错误码正在分领域收敛。** 部署前置条件、Kubernetes 目标授权与预览用途、Git 远程仓库/分支/冲突状态机、生成任务队列（取消、重试、优先级与死信重放）、生成配额、项目访问/删除、用户、角色与项目组管理、数据源成员与审批人约束、定时任务状态机、预览状态、文件读取限制，以及数据库审批、传输和备份完整性校验已提供稳定 code，并形成错误契约文档；其余参数校验和低频接口仍主要按 HTTP 状态映射，后续应按领域渐进迁移，避免机械生成含义重复的错误码。
3. **分页迁移尚未覆盖全部列表。** 项目、用户、角色、团队、数据源、生成任务、部署记录、预览构建、数据库审批/传输、Java 任务执行和监控告警事件已统一分页；观测聚合等有界时间窗口仍使用专用响应结构，需要继续区分“可翻页明细”和“限窗聚合”。
4. **版本元数据已提供发布注入入口。** API 健康响应和平台健康页展示 `APP_VERSION`、`BUILD_GIT_SHA`、`BUILD_TIME` 与实际镜像引用；Helm 提供对应 values，后续由 CI 在制品构建和发布阶段注入不可变值。

> 自动化测试建设按当前阶段要求暂缓，不列入本轮优化范围。

## 八、设计约束

- 新运行时需同时提供 `ProjectRuntime`、沙箱镜像、脚手架约束、预览契约和部署模板。
- 项目资源访问必须同时经过平台权限和 `ProjectAccessService`。
- 长耗时任务应采用持久状态 + 队列/租约 + 幂等执行 + 心跳/取消 + 可恢复失败语义。
- 新敏感配置必须复用 `CryptoService`，列表不得返回密文，轮换必须覆盖新增表。
- 新外部调用必须设置超时、响应上限、审计和 SSRF/出口控制。
- 多副本生产部署不得假设本地单机文件系统，必须使用一致的共享存储语义。
